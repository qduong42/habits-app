# v1.1: Dump upgrades + Today split — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax. The v1 plan's **"Rules for every worker"** section (docs/superpowers/plans/2026-06-09-habits-app-v1.md, Rules 1–11) is binding here too, with branch **feat/v1.1-dump-and-today** substituted everywhere. Decisions source: `new_features.md` (grilled 2026-06-10) + `CONTEXT.md`.

**Goal:** Ship the three grilled features: discard-with-note, Dump task-first quick action + braindump History, Today two-section split.

**Contract changes (single source of truth):**
```ts
// InboxItem gains:
{ ..., discardNote: string | null }
// POST /inbox/:id/discard body becomes optional JSON: { note?: string /* ≤2000, trimmed; empty/absent = no note */ }
```

---

### Task 1: Discard-with-note (server + web)

**Files:** Modify `server/src/db/schema.ts` (+`discardNote` text nullable on inboxItems), migration, `server/src/inbox/{routes,service}.ts`, `server/test/inbox.test.ts`, `web/src/types.ts`, `web/src/pages/Inbox.tsx`, `web/src/components/TriageCard.tsx`.

- [x] **Step 1 (TDD):** failing tests — discard with `{note:'answered: yes'}` stores it; discard with empty/whitespace note or no body stores null; note >2000 → 400; note ignored on convert routes; InboxItem serializer returns `discardNote`.
- [x] **Step 2:** migration (`discard_note` text nullable); route accepts optional body (zod `{note?: trim ≤2000}` — treat `''` as absent); service stores on the discard update only.
- [x] **Step 3 (web):** replace `window.confirm` discard in BOTH the Dump list and the TriageCard 🗑 path with an inline note input: placeholder "Optional note — Enter to discard, Esc to cancel"; **Enter with empty input = discard without note (no dedicated skip button)**; Escape cancels. TriageCard advances after discard as before.
- [x] **Step 4:** verify + lint green; commit `feat: discard with optional answer note`.

### Task 2: Dump task-first quick action + braindump History

**Files:** Modify `web/src/pages/Inbox.tsx`, `web/src/hooks/useInbox.ts`, `web/src/format.ts`, `web/src/index.css`. Server: none expected (`GET /inbox?all=1` exists).

- [ ] **Step 1 — quick action:** per-item buttons become **→ Task (FIRST)**, → Habit, Discard. **→ Task converts immediately as a one-off undated task** via `POST /inbox/:id/convert-task {name}` (name = first line ≤200 chars, notes = full text — mirror the habit prefill rule): zero friction for the 90% case; scheduling happens later via the task's ⋯ Edit on Today or the Triage flow (which keeps its full pickers). Show a small "Task created ✓" inline confirmation; celebration on unlocks as with habit converts.
- [ ] **Step 2 — History:** collapsed section at the bottom of the Dump tab: "History" heading; one row per **dump date** (user-local from `createdAt`, newest first): "Jun 10 · 5 items", tap toggles expansion (aria-expanded). Expanded: that day's NON-OPEN items with capture time (HH:MM), status icon + label — 🗑 (+ the `discardNote` underneath when present), ✅ task / 🌱 habit; when `status==='converted'` but both links null → "converted (since deleted)". Data: `useInbox` gains an `all` variant (`['inbox','all']` query on `?all=1`, fetched lazily — only when History is first expanded); open items excluded from History (they're the list above).
- [ ] **Step 3:** verify + lint; commit `feat(web): task-first dump actions and braindump history`.

### Task 3: Today two-section split

**Files:** Modify `web/src/pages/Today.tsx`, `web/src/index.css`.

- [ ] **Step 1:** wrap the existing 📌 tasks block in a "✅ Tasks" mega-section and the habit category groups in a "🌱 Habits" mega-section: distinct section headers (bigger than category headers) and visually distinct containers (e.g. subtle tinted backgrounds/borders — tasks warm `#bf360c`-tinted, habits purple-tinted; keep the approved row styles untouched inside). Category sub-headers stay inside Habits. Tasks section keeps its hidden-when-empty rule; Habits section header shows `done/scheduled` across all habits. ⏳ Scheduled toggle stays inside the Tasks section.
- [ ] **Step 2:** verify + lint; phoneish sanity via build; commit `feat(web): two-section Today (tasks vs habits)`.

### Task 4: Mini QA + docs

- [ ] **Step 1:** curl smoke of discard-note + convert-task quick path (throwaway user, test DB, cleanup incl. user_achievements); mark the three features done in `new_features.md` (append "Shipped in v1.1 <date>" lines, don't delete the decisions); update `current_progress.md`. Commit `docs: v1.1 QA notes and progress`.
