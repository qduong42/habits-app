// Placeholder — the Today checklist (habits grouped by category, XP bar,
// tasks section) arrives in Tasks 9 and 26.
export default function Today() {
  return (
    <div>
      <h1 className="page-title">Today</h1>
      <p className="placeholder">Your daily checklist will show up here.</p>
    </div>
  );
}
