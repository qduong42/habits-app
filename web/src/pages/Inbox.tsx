// Placeholder — the tab is labeled "Dump" in the UI; DB/API keep "inbox".
// Quick capture + triage arrive in Tasks 16 and 27.
export default function Inbox() {
  return (
    <div>
      <h1 className="page-title">Dump</h1>
      <p className="placeholder">Mind full? Soon you can dump it here — then schedule it.</p>
    </div>
  );
}
